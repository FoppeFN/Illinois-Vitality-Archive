<<<<<<< HEAD
name: Full Python Quality Gates

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]
=======
<<<<<<< HEAD
name: Full Python Automated Quality Gates

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
>>>>>>> origin/main
  workflow_dispatch:

jobs:
  quality-checks:
    runs-on: ubuntu-latest

<<<<<<< HEAD
    services:
      postgres:
        image: postgres:18
        env:
          POSTGRES_DB: IIVRDAPPDB
          POSTGRES_USER: IIVRDUSER
          POSTGRES_PASSWORD: IIVRDPASSWORDSECRET
        ports:
          - 5432:5432
        options: >-
          --health-cmd="pg_isready -U IIVRDUSER -d IIVRDAPPDB"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=10

    env:
      POSTGRES_DB: IIVRDAPPDB
      POSTGRES_USER: IIVRDUSER
      POSTGRES_PASSWORD: IIVRDPASSWORDSECRET
      POSTGRES_HOST: 127.0.0.1
      POSTGRES_PORT: "5432"
      DJANGO_SECRET_KEY: CI_SECRET_KEY
      DJANGO_DEBUG: "0"

=======
>>>>>>> origin/main
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
<<<<<<< HEAD
          python-version: "3.12"
=======
          python-version: "3.14"
>>>>>>> origin/main

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
<<<<<<< HEAD
          pip install -r requirements-dev.txt

      - name: Run migrations
        run: |
          python manage.py migrate

      - name: Run Flake8
        run: |
          flake8 .

      - name: Run mypy
        run: |
          mypy . || true

      - name: Run Bandit
        run: |
          bandit -r . -c bandit.yaml || true

      - name: Run tests with coverage
        run: |
          coverage run -m pytest
          coverage report --fail-under=50
          coverage xml

=======
          pip install flake8 mypy bandit coverage pytest

      # -------------------------
      # Linting (Flake8)
      # -------------------------
      - name: Run Flake8 (Linting)
        run: |
          flake8 .

      # -------------------------
      # Static Analysis (mypy)
      # -------------------------
      - name: Run mypy (Static Type Checking)
        run: |
          mypy .

      # -------------------------
      # Security Scanning (Bandit)
      # -------------------------
      - name: Run Bandit (Security Analysis)
        run: |
          bandit -r . -c bandit.yaml

      # -------------------------
      # Testing + Coverage
      # -------------------------
      - name: Run tests with coverage
        run: |
          coverage run -m pytest
          coverage report --fail-under=80
          coverage xml

      # Optional: upload coverage artifact
>>>>>>> origin/main
      - name: Upload coverage report
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report
<<<<<<< HEAD
          path: coverage.xml
=======
          path: coverage.xml
=======
name: Full Python Automated Quality Gates

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  quality-checks:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.14"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install flake8 mypy bandit coverage pytest

      # -------------------------
      # Linting (Flake8)
      # -------------------------
      - name: Run Flake8 (Linting)
        run: |
          flake8 .

      # -------------------------
      # Static Analysis (mypy)
      # -------------------------
      - name: Run mypy (Static Type Checking)
        run: |
          mypy .

      # -------------------------
      # Security Scanning (Bandit)
      # -------------------------
      - name: Run Bandit (Security Analysis)
        run: |
          bandit -r . -c bandit.yaml

      # -------------------------
      # Testing + Coverage
      # -------------------------
      - name: Run tests with coverage
        run: |
          coverage run -m pytest
          coverage report --fail-under=80
          coverage xml

      # Optional: upload coverage artifact
      - name: Upload coverage report
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report
          path: coverage.xml
>>>>>>> origin/Nicks-Branch
>>>>>>> origin/main
